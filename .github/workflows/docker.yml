name: Docker Image

on:
  push:
    bra:contentReference[oaicite:7]{index=7}

permissions:
  contents: read
  packages: write  # 推 GHCR 需要

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3
        # v3.12.0 是官方最新系列之一 :contentReference[oaicite:8]{index=8}

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
        # docker/login-action 官方用法 :contentReference[oaicite:9]{index=9}

      - name: Build and push (backend)
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          push: true
          tags: |
            ghcr.io/${{ github.repository }}/backend:latest
            ghcr.io/${{ github.repository }}/backend:${{ github.sha }}
        # build-push-action 官方说明 :contentReference[oaicite:10]{index=10}
